#
# @PROJECT_NAME@ - OpenWRT package (Makefile only, source from GitHub)
#
# Source timestamp: @CHAN_SOURCE_TS@
# Version: @CHAN_VER_STR@
#
# This file was generated by @CMAKE_COMMAND@ version @CMAKE_VERSION@.
#
include $(TOPDIR)/rules.mk
 
PKG_NAME:=@PROJECT_NAME@
 
PKG_SOURCE_PROTO:=git
PKG_SOURCE_URL:=https://github.com/RoEdAl/@PROJECT_NAME@
PKG_SOURCE_DATE:=@PROJECT_VERSION_MAJOR@-@PROJECT_VERSION_MINOR@-@PROJECT_VERSION_PATCH@
PKG_RELEASE:=$(AUTORELEASE)
PKG_SOURCE_VERSION:=@CHAN_SOURCE_VERSION@

PKG_LICENSE:=GPL-2.0
PKG_LICENSE_FILES:=COPYRIGHT.txt LICENSE.md
PKG_MAINTAINER:=Edmunt Pienkowsky <roed@onet.eu>
PKG_INSTALL:=1

MODULES_DIR:=/usr/lib/asterisk/modules

include $(INCLUDE_DIR)/package.mk
include $(INCLUDE_DIR)/cmake.mk
 
define Package/@PROJECT_NAME@
  SUBMENU:=Telephony
  SECTION:=net
  CATEGORY:=Network
  URL:=http://github.com/RoEdAl/@PROJECT_NAME@
  DEPENDS:=asterisk +alsa-lib +libsqlite3
  EXTRA_DEPENDS:=kmod-usb2 kmod-usb-acm kmod-usb-serial kmod-usb-serial-option kmod-usb-audio usb-modeswitch
  TITLE:=Quectel module support
endef
 
define Package/@PROJECT_NAME@/description
  Asterisk channel driver for Quectel and SimCOM modules.
endef

define Package/@PROJECT_NAME@/conffiles
/etc/asterisk/quectel.conf
endef

define Package/@PROJECT_NAME@/install
  $(INSTALL_DIR) $(1)/etc/asterisk
	$(INSTALL_DATA) $(PKG_BUILD_DIR)/ipkg-install/etc/asterisk/quectel.conf $(1)/etc/asterisk
	$(INSTALL_DIR) $(1)$(MODULES_DIR)
	$(INSTALL_BIN) $(PKG_BUILD_DIR)/ipkg-install/$(MODULES_DIR)/chan_quectel.so $(1)$(MODULES_DIR)
endef

define Package/@PROJECT_NAME@/postinst
#!/bin/sh
if [ -z "$${IPKG_INSTROOT}" ]; then
  echo
  echo 'o-------------------------------------------------------------------o'
  echo '| asterisk-chan-quectel notes                                       |'
  echo 'o-------------------------------------------------------------------o'
  echo '| Adding the "asterisk" user to the "dialout" group might be        |'
  echo '| required for Asterisk to be able to access the module.            |'
  echo 'o-------------------------------------------------------------------o'
  echo '| Adding the "asterisk" user to the "audio" group might be          |'
  echo '| required for Asterisk to be able to access the audio device.      |'
  echo 'o-------------------------------------------------------------=^_^=-o'
  echo
fi
exit 0
endef
 
$(eval $(call BuildPackage,@PROJECT_NAME@))

